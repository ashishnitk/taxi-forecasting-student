# Core data stack
pandas==2.2.2
numpy==1.26.4
pyarrow==15.0.2
python-dateutil==2.9.0.post0
holidays==0.53

# Modeling
scikit-learn==1.5.0
xgboost==2.0.3
lightgbm==4.3.0

# Hyperparameter tuning
optuna==3.6.1

# Experiment tracking & explainability
mlflow==2.14.1
shap==0.45.1
matplotlib==3.9.0

# Config / utilities
pyyaml==6.0.1
python-dotenv==1.0.1
requests==2.32.3

# Serving
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0

# Observability - Prometheus /metrics for the API
prometheus-fastapi-instrumentator==7.0.0

# Linting (development)
ruff==0.5.5

# Notebook and plot support
jupyterlab==4.6.0
nbformat==5.10.4
nbconvert==7.17.1
ipykernel==7.3.0
Pillow==10.4.0

# Testing
pytest==8.2.2
pytest-cov==5.0.0

# Build/runtime shim: provides pkg_resources for transitive deps (e.g. mlflow)
# on Python images that no longer bundle setuptools by default. Pin below 81
# because setuptools 81 removed the pkg_resources module.
setuptools<81
