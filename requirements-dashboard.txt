# dashboard dependencies (kept separate from the serving image).
#
# The Streamlit showcase talks to the API over HTTP, so it needs no models,
# MLflow or heavy ML stack — only the UI + plotting + HTTP libraries.
pandas==2.2.2
requests==2.32.3
streamlit==1.37.1
plotly==5.23.0
